eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: education-center
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
server:
  port: 8091
