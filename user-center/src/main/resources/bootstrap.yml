server:
  port: 8771

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: user-center
  cloud:
    config:
      name: user-center
      profile: dev
      discovery:
        enabled: true
        service-id: config-server

